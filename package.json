{
  "name": "make-runner-mcp",
  "version": "2.1.0",
  "type": "module",
  "description": "Scoped MCP server exposing only make up / make test / make composer for a project.",
  "main": "server.js",
  "bin": {
    "make-runner-mcp": "./server.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  }
}
