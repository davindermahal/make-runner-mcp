build: ## Build the docker image (only reachable via the also-read marker, not a real include)
	@echo building from $$(basename $$(pwd))

deploy-app: ## Deploy the app (must stay blocked by the denylist)
	@echo this must never print through the MCP tool
